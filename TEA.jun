module TEA

open(NeoPixel)

let mut state : Model<1, 150> = initialState(
    [
        { pin := 7 }
    ], 
    150
)

let mut previousAction : maybe<Action> = nothing()

fun setup() = {
    # Serial.begin(9600); #
    # Serial.println("Set color"); #
    ()
}
fun loop() = {
    Signal:foldP(
        update,
        inout state,
        actions(inout previousAction)
    )
}